import React from 'react';
import PropTypes from 'prop-types';
import SeriesPageContainer from '../../containers/SeriesPageContainer';
const SeriesPageRoute = props => {
  return <SeriesPageContainer />;
};

SeriesPageRoute.propTypes = {};

export default SeriesPageRoute;
